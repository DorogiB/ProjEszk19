jobs:

  include:
    - stage: test_backend
      language: java
      sudo: false
      script: 
        - cd BACKEND
        - mvn test -B
    
    - stage: build_frontend
      name: "Frontend build"
      language: node_js
      node_js:
        - '6.7.0'
      install:
        - cd FRONTEND
        - npm install
      script:
        - npm run compodoc
      
      

stages:
  - build_frontend
